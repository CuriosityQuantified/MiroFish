# ===========================================
# MiroFish Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core Frameworks =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM Related =============
# OpenAI SDK (unified OpenAI format for LLM calls)
openai>=1.0.0

# ============= Knowledge Graph (Graphiti + Kuzu) =============
# Self-hosted, embedded — replaces Zep Cloud
# Pinned: PatchedKuzuDriver in knowledge_graph.py targets this version.
# Test before upgrading — see PatchedKuzuDriver docstring.
graphiti-core[kuzu,anthropic]==0.28.2

# ============= Zep Cloud (LEGACY — optional fallback, not imported at runtime) =============
# zep-cloud==3.13.0  # Uncomment only if you need the Zep fallback path

# ============= OASIS Social Media Simulation =============
# Social simulation (optional, ~500MB): pip install -r requirements-simulation.txt

# ============= File Processing =============
PyMuPDF>=1.24.0
# Encoding detection (supports non-UTF-8 encoded text files)
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= MCP Server =============
# FastMCP — MCP tool server (Phase 4)
mcp[cli]>=1.0.0

# ============= Utilities =============
# Environment variable loading
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0
